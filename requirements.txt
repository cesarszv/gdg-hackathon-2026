# GreenSpark backend dependencies (Python 3.14)
# Pinned lower bounds; exact versions captured in requirements.lock after install.

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-dotenv>=1.0.0

# Persistence (existing database/ package)
SQLAlchemy>=2.0.0

# Deterministic data layer
numpy>=2.0.0
pandas>=2.2.0

# Optional OpenAI advisor gateway called via httpx
httpx>=0.27.0

# Testing
pytest>=8.0.0
