[pytest]
pythonpath = .
testpaths = tests
addopts = -q
filterwarnings =
    # External: Starlette TestClient warns about httpx; not actionable in our code.
    ignore:Using `httpx` with `starlette.testclient`
